import { Galeria } from "./Galeria";

/**
 * Galeria de pantalles (només desenvolupament, vegeu layout.tsx).
 * Cada pantalla registrada a pantalles.tsx es mostra dins d'un marc de mòbil
 * amb un iframe a /pantalles/vista/<id>.
 */
export default function PantallesPage() {
  return <Galeria />;
}
